/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'hydeparkwinterwonderland.com', 'wembleypark.com', 'raw.githubusercontent.com', 'raw.githubusercontent.com'],
  }
}

module.exports = nextConfig
